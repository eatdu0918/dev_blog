/**
 * 사용자의 연령이 성인 기준(18세)에 부합하는지 확인하는 간단한 함수입니다.
 * 뮤테이션 테스팅의 효과를 보여주기 위해 의도적으로 경계값을 포함하는 로직을 사용합니다.
 */
export function isAdult(age: number): boolean {
  // 18세 이상이면 성인으로 간주합니다.
  // 뮤테이션 테스팅 도구는 이 '>=' 연산자를 '>'나 '<' 등으로 바꾸어 테스트를 수행합니다.
  return age >= 18;
}
